> 前期方案记录：初始化后的当前目录、接口、状态与研究层规范见 [docs/README.md](docs/README.md)。原建议保留供追溯。

# AI 辅助开发规范

AI 是生产工具，不是事实来源或项目决策者。所有 AI 生成内容、代码、图片、声音都必须经过人工审阅，并记录工具、提示（如需）、输入来源和许可证风险。

## 让 AI 不失控
- 每个模块先读 manifest、schema、README、SOURCES 和 ADR，再修改。
- 任务按单模块、单包、单文档拆分；禁止跨边界顺手重构。
- 先改 schema/契约，再改实现；保持小 PR 和可回滚提交。
- 生成内容必须带 `draft` 状态，未审核不得发布。
- 代码必须通过类型、lint、单测、E2E、可访问性和资产检查。
- AI 不得删除来源、测试、许可证、降级视图或错误边界来“让构建通过”。

## 研究与生成记录
在 `docs/research/` 或模块 `SOURCES.md` 记录来源和审核人；在 `assets/manifest.json` 记录生成工具、作者、输入许可、输出许可和哈希。涉及真实人物、社群或传统知识时，必须由合适的人工贡献者确认。
