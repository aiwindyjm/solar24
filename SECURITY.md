# Security

当前为本地静态工程骨架，无账户或后端。不要把 API key、私人资料或漏洞利用细节提交到公开 Issue。远程仓库上线后，维护者应启用 GitHub Private Vulnerability Reporting 并在此补充真实联系渠道。

依赖由 pnpm lockfile 固定；外部表达不允许运行远程任意 JS。Obsidian 插件和工作区状态默认忽略。敏感文件误入历史时须撤销凭据并协调清理历史，不能仅靠 .gitignore。
