# 2026-09-20 · 介绍书与历日漫游验收

## 最新：手卷与听人间分工

本轮位于第五版 `src/ReadingScroll.tsx`、`src/Life.tsx`、`main.tsx`、`Book.tsx`、`state.ts`、`style.css` 及字库子集/说明文档。取消上一轮“阅读时左移缩小年轮”的处理。以下旧阅读卡片记录仅保留迭代历史，以本节为准。

- 观四时独占五章文化阅读：右轴固定、左轴随裁切边界左移，纸面以 850ms 过渡向左铺开，反向收起；移动端仍采用视口内手卷，内容独立滚动。减少动态模式无过渡。闭合内容使用 inert 与 aria-hidden，关闭后返回入口焦点。
- 展卷前后年轮 bounds 实测完全一致：1269×1066 视口中 x=317.25、y=98、width=height=685.25。中间帧 clip-path 左裁切约 21.09%，确认实际向左展开，而非整体缩放或移动蝴蝶。
- 听人间与望天地不再渲染五类文化入口，也无共用阅读卷。听人间的水岸、田间、灯下分别提供合成声、四阶段生长滑杆、私人节气记忆。参与时隐藏其他景物标记，退回场景再选择。
- 浏览器验证：主动开启/关闭声音；滑杆 End 键到收成；180 字符记忆保存、刷新恢复、清空保存移除；双语切换；手机无横向溢出；Escape 收卷；减少动态零过渡；一个 canvas；无 pageerror。测试记忆已清除。
- 1269×1066、1280×720、390×844 截图已检查，文件位于 `output/playwright/solar24-v5-scroll-{open,unrolling,mobile,720}.png`、`solar24-v5-life-{field,memory-zh,mobile,listen-mobile,listen-720}.png`。
- `pnpm lint`、74 项测试、独立 V5 TypeScript、仓库 build、V5 build 通过。Host E2E 经 4183 临时配置运行，桌面/手机 2 项通过。最终 V5 JS 1,119.82 kB / gzip 336.74 kB；仍有原有大 chunk 提醒和 Host Zod 注释提示。

新交互文字为项目观察邀请与写意体验，不新增民俗考证结论。声音仍为合成环境音，记忆仅在当前浏览器保存，无上传服务。

## 阅读卡片视觉跟进

按页面批注仅修改第五版 `src/style.css`：桌面阅读栏由 340px 改为 420–540px 自适应宽度（1269px 视口实测 482.22px），46% 环境色底与 10px 背景模糊，细滚动条和轻边界；星空采用深色半透明底与浅文字。不支持背景模糊时回退到较实的底色。宽屏阅读时年轮稍缩小并左移，给蝴蝶主体和正文各留空间。手机外边距降至 4%，390px 视口阅读区宽 358.81px，保持文档流布局。

浏览器复核观四时、听人间、望天地，1269×1066、1280×720、390×844，无手机水平溢出，章节开合、关闭正常。截图：`output/playwright/solar24-v5-glass-{seasons,life,cosmos,720,mobile}.png`。`pnpm lint`、74 项测试、仓库 build 与第五版独立 build 通过；原有 Zod 注释与大 chunk 构建提示仍在。未修改导航逻辑，未新增测试文件。

## 介绍书与漫游实现验收

本轮修改范围及内容来源见 `BOOK.md`。最终检查：

- `pnpm lint` 通过，验证 24 模块、586 知识实体和 13 模板。
- `pnpm test`：8 文件、74 项测试通过（新增第五版时间轴、历史恢复和静态章节引用检查共 5 项）。
- `pnpm build` 通过；Zod 的 Rollup 注释提示仍存在。
- `pnpm exec tsc -p docs/design/solar24-v5/tsconfig.json` 通过。
- 本轮修改文件的 Prettier 检查通过；扩大到整个第五版 src 时，未修改的 `Scene.tsx` 存在既有格式提示，本轮没有为消除该提示重排它。
- `pnpm exec vite build --config docs/design/solar24-v5/vite.config.ts` 通过。最终 JS 1,113.17 kB / gzip 333.10 kB；仍有大于 500 kB 的 chunk 警告，没有调高阈值掩盖它。
- `pnpm test:e2e --config .tmp/solar24-v3-e2e.config.ts`：Host 桌面、手机 2 项通过，使用 4183 避让既有预览服务；它不代替第五版实测。

第五版 Chromium 实测：

- Playwright 时钟走完 365 个日步：立春起点、雨水边界、12 月进入 2027 年、两寒与回到立春都正确；打开日历继续推进并同步选中格。
- 暂停、模拟页面隐藏停止历日；恢复不追赶跳日；手动日期选择结束漫游。减少动态设置下可主动开始离散历日漫游。
- 实际计时复核：打开日历后 1.5 秒内日期从 04 到 06；再次实测从立春跨入雨水成功。不是仅靠纯函数测试判断 UI。
- 手动选择 2 月 12 日，切至 Cosmos 后浏览器返回、前进、刷新均保留 12 日。
- 逐个访问 24 节气，五章结构和生活正文均成功渲染；中英切换、独立展开、键盘 Enter 收起、外部分类重新打开、来源展开通过。修复分类变化与章节收起之间的状态竞争。
- 1258×1066、1280×720、390×844 检查通过；手机阅读区域在文档流中，没有水平溢出；年份按钮字号单独限制，避免挤压日历标题。
- 新会话检查没有 pageerror，始终一个 canvas。控制台记录仅见开发环境 React DevTools 提示。

截图保存在 `output/playwright/`：`solar24-v5-book-en.png`、`solar24-v5-book-zh.png`、`solar24-v5-book-720.png`、`solar24-v5-book-mobile-en.png`、`solar24-v5-book-mobile-zh.png`、`solar24-v5-book-calendar.png`。

限制：历日只内置 2026–2027 年，精度为日、参考 UTC+8；年轮日步插值不是精确黄经。文化正文与英文仍是待人工核验的编辑稿。未进行真实低端手机性能评测或跨浏览器验收。
