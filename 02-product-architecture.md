> 前期方案记录：初始化后的当前目录、接口、状态与研究层规范见 [docs/README.md](docs/README.md)。原建议保留供追溯。

# 产品架构

## 用户路径
1. 根入口说明“什么是二十四节气”，提供季节时间轴，并明确提出开放邀请：“这个时间在你的文化里意味着什么？”
2. 用户进入一个节气模块，先看到一句话和可选轻量视觉，再按 What / Why / Nature / People / Culture 逐层理解中国表达。
3. Experience 区把关键事实转成动画、声音或交互，并提供无动画、无声音和低性能替代路径。
4. Sources 展示经过审核的资料；随后同时提供“继续了解中国”和“分享你的季节经验”两条入口。社区投稿后置实现，但邀请和协议从首版信息架构存在。

## 产品层次
- **Solar24 Host**：首页、季节导航、模块发现、加载状态、语言和无障碍设置。
- **Official Terms**：24 个中国节气模块，具有统一叙事和事实审核状态。
- **Community Expressions**：按节气挂载的社区作品，标注地点、作者、关联理由、来源、媒体和许可证；首版可以先展示协议和投稿入口。
- **Archive/Protocol**：机器可读索引、贡献指南和版本化协议。

## 推荐方案与取舍
推荐“内容驱动的模块 + 共享运行时 + 静态构建”。内容和体验声明放在模块目录，通用渲染器放在 packages，Host 通过 manifest 注册模块。这样可以避免 24 份重复导航、音频控制和数据解析代码。暂不采用微前端、后端 CMS 或每节气独立部署；这些会增加首版复杂度且不能改善文化理解。

## MVP 边界
先完成协议、数据校验、Host 壳、立春参考模块、一个可访问的二维/轻量动效体验、README 和 CI。其余 23 个模块先生成经过 schema 校验的内容骨架，不伪造完整文化内容或视觉。
