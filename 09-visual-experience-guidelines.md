> 前期方案记录：初始化后的当前目录、接口、状态与研究层规范见 [docs/README.md](docs/README.md)。原建议保留供追溯。

# 视觉、声音与交互规范

## 核心规则
每个效果必须回答“帮助用户理解了什么”。先确定文化事实和叙事，再选择视觉媒介。不要用粒子、3D、音乐掩盖缺少解释的内容。

## 设计系统
共享 token：色彩语义、字号、间距、圆角、动效时长、对比度、焦点样式和断点；模块可定义季节调色和材质，但不能改变阅读、导航和无障碍契约。视觉规范放在 `packages/ui` 与 `docs/design/`，模块只声明主题 token。

## 体验层级
静态叙事 → 轻交互图表/时间轴 → Canvas/WebGL → 声音。设备性能、网络、用户设置决定是否升级。所有动画支持 `prefers-reduced-motion`，声音默认静音并由用户手势启用，提供字幕和文本描述。

## 评审问题
效果是否对应可引用事实？用户能否暂停、跳过和返回？键盘、触摸、屏幕阅读器能否完成同一理解路径？低端设备是否仍能阅读？资产是否有来源、许可证和体积预算？
